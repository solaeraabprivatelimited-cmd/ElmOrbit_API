services:
  - type: web
    name: lernova-monitoring
    env: python
    pythonVersion: 3.11
    buildCommand: pip install -r monitoring_requirements.txt
    startCommand: uvicorn monitoring_server:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
